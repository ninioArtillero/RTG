---
# Punto de entrada de la maqueta, llamada por pandoc desde la Makefile con la flag '--defaults'
# Contiene las opciones generales de pandoc para la generación del documento.

standalone: true

verbosity: WARNING

citeproc: true

number-sections: true

reference-links: true

top-level-division: chapter

from: markdown+rebase_relative_paths

pdf-engine: xelatex

## Syntax = pygments | tango | espresso | zenburn | kate | monochrome | breezedark | haddock
## haddock + background tango
highlight-style: ${.}/syntax-highlight.theme

## Aplicar por defecto a todo el código indentado
indented-code-classes:
- haskell
- numberline

metadata-files:
- ${.}/metadata.yaml
- ${.}/document.yaml

#pdf-engine-opts:
#  # Para utilizar paquete 'minted' para marcar código fuente.
#  - '-shell-escape'
...
